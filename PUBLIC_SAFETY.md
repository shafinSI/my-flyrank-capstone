# Public-safety pass

- Synthetic site URLs in copied notebooks were replaced with `example.com`.
- No private queries or credentials were added.
- The required FlyRank credit link is preserved: https://flyrank.ai
- Production data was not fabricated or claimed.

Before final submission, verify the live GitHub Pages URL in `submission/paper_url.txt` exactly matches the URL GitHub shows under Settings → Pages.
